syntax = "proto3";

service GeoIpService {
    rpc getCity(GeoIpRequest) returns (GeoIpResponse) {}
}

message GeoIpRequest {
    string ip = 1;
}

message GeoIpResponse {
    string ip = 1;
    string country = 2;
    string city = 3;
    float latitude = 4;
    float longitude = 5;
}
